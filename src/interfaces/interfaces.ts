export interface Article {
    category: string;
    title: string;
    imageBg?: string;
    bgColor?: string;
    author: string;
    date?: string;
    imageAuthor: string;
    featured?: boolean;
    audio?: boolean;
    support?: boolean;
}